(function () {
  'use strict';

  angular
    .module('MyApp')
    .config(config);

  /* @ngInject */
  function config(facebookProvider) {
//    facebookProvider.init(/* Facebook App Id Here */);
  }
})();
